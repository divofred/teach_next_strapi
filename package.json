{
  "name": "linxas",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^20.3.1",
    "husky": "^8.0.3",
    "lerna": "^6.6.2",
    "lint-staged": "^13.2.2",
    "prettier": "^2.8.8",
    "@trivago/prettier-plugin-sort-imports": "^3.3.0",
    "prettier-plugin-css-order": "^1.3.0"
  },
  "scripts": {
    "develop": "lerna run --scope backend --scope frontend develop --stream",
    "develop:backend": "lerna run --scope backend develop",
    "develop:frontend": "lerna run --scope frontend develop"
  }
}
